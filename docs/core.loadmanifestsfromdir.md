<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@macts/core](./core.md) &gt; [loadManifestsFromDir](./core.loadmanifestsfromdir.md)

## loadManifestsFromDir() function

Load every manifest under a manifests root directory.

Each immediate subdirectory containing an `app.yaml` is treated as one app. Subdirectories without an `app.yaml` are skipped. Manifests that fail to load are collected as errors rather than aborting the whole load, so one malformed manifest does not break discovery for the rest.

**Signature:**

```typescript
declare function loadManifestsFromDir(manifestsDir: string): Promise<{
  manifests: AppManifest[]
  errors: {
    app: string
    message: string
  }[]
}>
```

## Parameters

<table><thead><tr><th>

Parameter

</th><th>

Type

</th><th>

Description

</th></tr></thead>
<tbody><tr><td>

manifestsDir

</td><td>

string

</td><td>

Path to the manifests root (e.g. `<repo>/manifests`<!-- -->)

</td></tr>
</tbody></table>

**Returns:**

Promise&lt;{ manifests: [AppManifest](./core.appmanifest.md)<!-- -->\[\]; errors: { app: string; message: string; }\[\]; }&gt;

Loaded manifests and any per-manifest load errors
