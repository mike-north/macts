<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@macts/core](./core.md) &gt; [buildResourceCommandRoute](./core.buildresourcecommandroute.md)

## buildResourceCommandRoute() function

Build the canonical route for a resource-scoped command.

Resource commands are addressed as `{app}.{resourcePlural}.{commandKey}`<!-- -->.

**Signature:**

```typescript
declare function buildResourceCommandRoute(
  appName: string,
  resourcePlural: string,
  commandKey: string
): string
```

## Parameters

<table><thead><tr><th>

Parameter

</th><th>

Type

</th><th>

Description

</th></tr></thead>
<tbody><tr><td>

appName

</td><td>

string

</td><td>

The raw `manifest.app.name`<!-- -->.

</td></tr>
<tr><td>

resourcePlural

</td><td>

string

</td><td>

The resource's `plural` name from the manifest.

</td></tr>
<tr><td>

commandKey

</td><td>

string

</td><td>

The command's key in `manifest.commands` (NOT `command.name`<!-- -->). The key is globally unique within the manifest, whereas `name` is shared across resources, so two distinct operations (`list` for Calendar, `listEvents` for Event) would otherwise collide.

</td></tr>
</tbody></table>

**Returns:**

string

The canonical route string.
