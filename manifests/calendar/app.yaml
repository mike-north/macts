version: '1.0'

app:
  bundleId: com.apple.iCal
  name: Calendar
  displayName: Calendar
  tccEntitlements:
    - calendar
    - automation
  distributionModel: system

suites:
  - name: Standard Suite
    code: '????'
    description: Common classes and commands for all applications
  - name: iCal
    code: wrbt
    description: iCal classes and commands
    resources:
      - Calendar
      - Event
      - Attendee
      - DisplayAlarm
      - MailAlarm
      - SoundAlarm
      - OpenFileAlarm
    commands:
      - reloadCalendars
      - switchView
      - viewCalendar
      - show
    enums:
      - ParticipationStatus
      - EventStatus
      - CalendarPriority
      - ViewType

resources:
  Calendar:
    name: Calendar
    plural: Calendars
    description: A calendar containing events
    code: wres
    # The dictionary-declared `calendarIdentifier` is NOT runtime-valid: JXA
    # throws on `calendar.calendarIdentifier()` (and `uid`/`id`/`properties()`).
    # Only `name` works at runtime, so the calendar is targeted by name via a
    # `whose({ name: ... })[0]` lookup rather than `byId(...)`.
    identifiers:
      - property: name
        primary: true
        targeting: byProperty
    properties:
      name:
        access: rw
        type: string
        description: The calendar title
        code: pnam
      title:
        access: rw
        type: string
        description: The calendar title (synonym for name)
        code: wr02
        deprecated:
          message: Use 'name' instead
          upstream: true
      color:
        access: rw
        type: rgb
        description: The calendar color
        code: colr
      calendarIdentifier:
        access: r
        type: string
        description: A unique calendar key
        code: 'ID  '
      writable:
        access: r
        type: boolean
        description: Whether the calendar can be modified
        code: wr05
      description:
        access: rw
        type: string
        description: The calendar description
        code: wr12

  Event:
    name: Event
    plural: Events
    description: A calendar event
    code: wrev
    identifiers:
      - property: uid
        primary: true
    properties:
      summary:
        access: rw
        type: string
        description: The event summary/title
        code: wr11
      description:
        access: rw
        type: string
        description: The event notes
        code: wr12
      location:
        access: rw
        type: string
        description: The event location
        code: wr14
      startDate:
        access: rw
        type: date
        description: The event start date
        code: wr1s
      endDate:
        access: rw
        type: date
        description: The event end date
        code: wr5s
      alldayEvent:
        access: rw
        type: boolean
        description: True if the event is an all-day event
        code: wrad
      recurrence:
        access: rw
        type: string
        description: The iCalendar (RFC 2445) string describing the event recurrence, if defined
        code: wr15
      status:
        access: rw
        type:
          enum: EventStatus
        description: The event status
        code: wre4
      sequence:
        access: r
        type: integer
        description: The event version
        code: wr13
      stampDate:
        access: rw
        type: date
        description: The event modification date
        code: wr4s
      excludedDates:
        access: rw
        type:
          array: date
        description: The exception dates for recurring events
        code: wr2s
      uid:
        access: r
        type: string
        description: A unique event key
        code: 'ID  '
      url:
        access: rw
        type: string
        description: The URL associated with the event
        code: wr16

  Attendee:
    name: Attendee
    plural: Attendees
    description: An event attendee
    code: wrea
    properties:
      displayName:
        access: r
        type: string
        description: The first and last name of the attendee
        code: wra1
      email:
        access: r
        type: string
        description: Email address of the attendee
        code: wra2
      participationStatus:
        access: r
        type:
          enum: ParticipationStatus
        description: The invitation status for the attendee
        code: wra3

  DisplayAlarm:
    name: DisplayAlarm
    plural: DisplayAlarms
    description: A message/display alarm
    code: wal1
    properties:
      triggerInterval:
        access: rw
        type: integer
        description: The interval in minutes between the event and the alarm (positive for after, negative for before)
        code: wald
      triggerDate:
        access: rw
        type: date
        description: An absolute alarm date
        code: wale

  MailAlarm:
    name: MailAlarm
    plural: MailAlarms
    description: A mail/email alarm
    code: wal2
    properties:
      triggerInterval:
        access: rw
        type: integer
        description: The interval in minutes between the event and the alarm (positive for after, negative for before)
        code: wald
      triggerDate:
        access: rw
        type: date
        description: An absolute alarm date
        code: wale

  SoundAlarm:
    name: SoundAlarm
    plural: SoundAlarms
    description: A sound alarm
    code: wal4
    properties:
      triggerInterval:
        access: rw
        type: integer
        description: The interval in minutes between the event and the alarm (positive for after, negative for before)
        code: wald
      triggerDate:
        access: rw
        type: date
        description: An absolute alarm date
        code: wale
      soundName:
        access: rw
        type: string
        description: The system sound name to be used for the alarm
        code: wals
      soundFile:
        access: rw
        type: string
        description: The (POSIX) path to the sound file to be used for the alarm
        code: walf

  OpenFileAlarm:
    name: OpenFileAlarm
    plural: OpenFileAlarms
    description: An 'open file' alarm
    code: wal3
    properties:
      triggerInterval:
        access: rw
        type: integer
        description: The interval in minutes between the event and the alarm (positive for after, negative for before)
        code: wald
        deprecated:
          message: Starting with OS X 10.14, it is not possible to create new open file alarms. Trying to save or modify an open file alarm will result in a save error.
          since: '10.14'
          upstream: true
      triggerDate:
        access: rw
        type: date
        description: An absolute alarm date
        code: wale
        deprecated:
          message: Starting with OS X 10.14, it is not possible to create new open file alarms. Trying to save or modify an open file alarm will result in a save error.
          since: '10.14'
          upstream: true
      filepath:
        access: rw
        type: string
        description: The (POSIX) path to be opened by the alarm
        code: walp
        deprecated:
          message: Starting with OS X 10.14, it is not possible to view URLs for existing open file alarms
          since: '10.14'
          upstream: true

enums:
  ParticipationStatus:
    name: ParticipationStatus
    description: Status of an attendee's response to an invitation
    code: wre6
    values:
      - name: unknown
        value: unknown
        description: No answer yet
        code: E6na
      - name: accepted
        value: accepted
        description: Invitation has been accepted
        code: E6ap
      - name: declined
        value: declined
        description: Invitation has been declined
        code: E6dp
      - name: tentative
        value: tentative
        description: Invitation has been tentatively accepted
        code: E6tp

  EventStatus:
    name: EventStatus
    description: Status of a calendar event
    code: wre4
    values:
      - name: cancelled
        value: cancelled
        description: A cancelled event
        code: E4ca
      - name: confirmed
        value: confirmed
        description: A confirmed event
        code: E4cn
      - name: none
        value: none
        description: An event without status
        code: E4no
      - name: tentative
        value: tentative
        description: A tentative event
        code: E4te

  CalendarPriority:
    name: CalendarPriority
    description: Priority level for calendar items
    code: wrp1
    values:
      - name: noPriority
        value: 0
        description: No priority
        code: tdp0
      - name: lowPriority
        value: 9
        description: Low priority
        code: tdp9
      - name: mediumPriority
        value: 5
        description: Medium priority
        code: tdp5
      - name: highPriority
        value: 1
        description: High priority
        code: tdp1

  ViewType:
    name: ViewType
    description: Calendar view type
    code: wre5
    values:
      - name: dayView
        value: dayView
        description: The iCal day view
        code: E5da
      - name: weekView
        value: weekView
        description: The iCal week view
        code: E5we
      - name: monthView
        value: monthView
        description: The iCal month view
        code: E5mo

hierarchy:
  children:
    calendars:
      resource: Calendar
      access: rw
      description: Calendars in the application
      children:
        events:
          resource: Event
          access: rw
          description: Events within a calendar
          children:
            attendees:
              resource: Attendee
              access: r
              description: Attendees of an event
            displayAlarms:
              resource: DisplayAlarm
              access: rw
              description: Display alarms for an event
            mailAlarms:
              resource: MailAlarm
              access: rw
              description: Mail alarms for an event
            soundAlarms:
              resource: SoundAlarm
              access: rw
              description: Sound alarms for an event
            openFileAlarms:
              resource: OpenFileAlarm
              access: rw
              description: Open file alarms for an event (deprecated)

commands:
  # Standard CRUD commands for calendars
  list:
    name: list
    description: List all calendars
    scope: resource
    resourceType: Calendar
    code: core
    parameters: []
    permission: calendar:calendars:list

  get:
    name: get
    description: Get a calendar by ID
    scope: resource
    resourceType: Calendar
    code: getd
    parameters:
      - name: id
        type: string
        description: Calendar identifier
        required: true
    permission: calendar:calendars:get

  create:
    name: create
    description: Create a new calendar
    scope: resource
    resourceType: Calendar
    code: crel
    parameters:
      - name: name
        type: string
        description: Calendar name
        required: true
      - name: color
        type: rgb
        description: Calendar color
        required: false
    permission: calendar:calendars:create

  # Event-specific CRUD commands
  listEvents:
    name: list
    description: List all events in a calendar
    scope: resource
    resourceType: Event
    code: core
    parameters:
      - name: calendarId
        type: string
        description: Calendar identifier
        required: true
    permission: calendar:events:list

  getEvent:
    name: get
    description: Get an event by ID
    scope: resource
    resourceType: Event
    code: getd
    parameters:
      - name: id
        type: string
        description: Event identifier
        required: true
    permission: calendar:events:get

  createEvent:
    name: create
    description: Create a new event
    scope: resource
    resourceType: Event
    code: crel
    parameters:
      - name: calendarId
        type: string
        description: Calendar identifier for the event
        required: true
      - name: summary
        type: string
        description: Event title
        required: true
      - name: startDate
        type: date
        description: Event start date
        required: true
      - name: endDate
        type: date
        description: Event end date
        required: true
      - name: location
        type: string
        description: Event location
        required: false
      - name: description
        type: string
        description: Event notes
        required: false
      - name: alldayEvent
        type: boolean
        description: Whether this is an all-day event
        required: false
    permission: calendar:events:create

  reloadCalendars:
    name: reloadCalendars
    description: Tell the application to reload all calendar files contents
    scope: application
    code: aec8
    parameters: []
    permission: calendar:calendars:reload

  switchView:
    name: switchView
    description: Show calendar on the given view
    scope: application
    code: aeca
    parameters:
      - name: to
        type: ViewType
        description: The calendar view to be displayed
        required: true
        code: wre5
    permission: calendar:app:switchView

  viewCalendar:
    name: viewCalendar
    description: Show calendar on the given date
    scope: application
    code: aec9
    parameters:
      - name: at
        type: date
        description: The date to be displayed
        required: true
        code: wtdt
    permission: calendar:app:viewCalendar

  show:
    name: show
    description: Show the event or to-do in the calendar window
    scope: resource
    resourceType: Event
    code: aec3
    parameters: []
    permission: calendar:events:show

# Permissions mapping - defines coarse→fine permission relationships
permissions:
  # Resource-level permissions
  calendars:
    read:
      - calendar:calendars:list
      - calendar:calendars:get
      - calendar:calendars:reload
    create:
      - calendar:calendars:create
    write:
      - calendar:calendars:update
    delete:
      - calendar:calendars:delete

  events:
    read:
      - calendar:events:list
      - calendar:events:get
      - calendar:events:show
    create:
      - calendar:events:create
    write:
      - calendar:events:update
    delete:
      - calendar:events:delete

  attendees:
    read:
      - calendar:attendees:list
      - calendar:attendees:get

  displayAlarms:
    read:
      - calendar:displayAlarms:list
      - calendar:displayAlarms:get
    create:
      - calendar:displayAlarms:create
    write:
      - calendar:displayAlarms:update
    delete:
      - calendar:displayAlarms:delete

  mailAlarms:
    read:
      - calendar:mailAlarms:list
      - calendar:mailAlarms:get
    create:
      - calendar:mailAlarms:create
    write:
      - calendar:mailAlarms:update
    delete:
      - calendar:mailAlarms:delete

  soundAlarms:
    read:
      - calendar:soundAlarms:list
      - calendar:soundAlarms:get
    create:
      - calendar:soundAlarms:create
    write:
      - calendar:soundAlarms:update
    delete:
      - calendar:soundAlarms:delete

  openFileAlarms:
    read:
      - calendar:openFileAlarms:list
      - calendar:openFileAlarms:get
    create:
      - calendar:openFileAlarms:create
    write:
      - calendar:openFileAlarms:update
    delete:
      - calendar:openFileAlarms:delete

  # Application-level permissions
  app:
    read:
      - calendar:app:switchView
      - calendar:app:viewCalendar

extraction:
  sourceFile: source.sdef
  confidence:
    overall: 0.95
    fields:
      resources: 1.0
      enums: 1.0
      hierarchy: 0.95
      commands: 0.95
  openQuestions:
    - question: Should CalendarPriority enum be exposed or is it unused in modern Calendar.app?
      context: The enum is defined in the SDEF but no properties reference it
      relatedTo: CalendarPriority
    - question: Are there additional Standard Suite commands (like make, delete, save) that should be explicitly documented?
      context: Standard Suite is included via xi:include reference
      suggestions:
        - Include standard commands explicitly
        - Reference Standard Suite documentation separately
